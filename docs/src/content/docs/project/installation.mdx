---
title: "Installation Steps"
---

# Installation

To install `react-native-performance-tracker`, use the following commands:

```bash
yarn add react-native-performance-tracker
cd ios && pod install
```

If you are using npm, run:

```bash
npm install react-native-performance-tracker
```